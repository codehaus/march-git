= render_favorite_icon(group) 
.info-panel-minimal.description
  - if group.description and not group.description.blank?
    :textile
      #{group.description}
  - else
    :textile
      No description provided
      

